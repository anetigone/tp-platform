<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tp.mapper.MessageMapper">

    <select id="getByConversationId" resultType="com.tp.common.entity.Message">
        select *
        from "message"
        where conversation_id = #{conversationId}
        order by create_time desc
    </select>

    <select id="getById" resultType="com.tp.common.entity.Message">
        select *
        from "message"
        where id = #{id}
    </select>

    <select id="listByConversationId" resultType="com.tp.common.entity.Message">
        select *
        from "message"
        where conversation_id = #{conversationId}
        order by update_time desc
        limit #{offset}, #{limit}
    </select>

    <select id="getMessageCount">
        select count(*)
        from "message"
        where conversation_id = #{conversationId}
    </select>

    <insert id="insert" parameterType="com.tp.common.entity.Message"
            useGeneratedKeys="true" keyProperty="id">
        insert into "message"(conversation_id, sender_id, receiver_id, content, message_type, is_read, create_time, update_time)
        values (#{conversationId}, #{senderId}, #{receiverId}, #{content}, #{messageType}, #{isRead}, #{createTime}, #{updateTime})
    </insert>

    <update id="batchMarkAsRead" parameterType="java.util.List">
        update "message"
        set is_read = 1
        where id in
        <foreach item="item" index="index" collection="messageIds" separator="," open="(" close=")">
            #{item}
        </foreach>
    </update>
</mapper>