<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tp.mapper.SquatMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id" parameterType="com.tp.common.entity.Squat">
        insert into squat(user_id, product_id, remind_type, create_time, update_time)
        values(#{userId}, #{productId}, #{remindType}, #{createTime}, #{updateTime})
    </insert>

    <update id="update" parameterType="com.tp.common.entity.Squat">
        update squat
        <set>
            <if test="remindType != null">
                remind_type = #{remindType},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
        </set>
        where id = #{id}
    </update>

    <delete id="deleteById">
        delete from squat
        where id = #{id}
    </delete>

    <select id="getById" resultType="com.tp.common.entity.Squat">
        select *
        from squat
        where id = #{id}
    </select>

    <select id="getAll" resultType="com.tp.common.entity.Squat">
        select *
        from squat
    </select>

    <select id="listByUserId" resultType="com.tp.common.entity.Squat">
        select *
        from squat
        where user_id = #{userId}
        limit #{offset}, #{limit}
    </select>

    <select id="countByUserId">
        select count(*)
        from squat
        where user_id = #{userId}
    </select>

    <select id="countByProductId">
        select count(*)
        from squat
        where product_id = #{productId}
    </select>

    <select id="listByProductId" resultType="com.tp.common.entity.Squat">
        select *
        from squat
        where product_id = #{productId}
        limit #{offset}, #{limit}
    </select>

    <select id="getByUserIdAndProductId" resultType="com.tp.common.entity.Squat">
        select *
        from squat
        where user_id = #{userId}
        and product_id = #{productId}
    </select>
</mapper>