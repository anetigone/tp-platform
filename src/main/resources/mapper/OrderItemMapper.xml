<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tp.mapper.OrderItemMapper">

    <!-- 结果映射 -->
    <resultMap id="OrderItemResultMap" type="com.tp.common.entity.OrderItem">
        <id column="id" property="id"/>
        <result column="order_id" property="orderId"/>
        <result column="product_id" property="productId"/>
        <result column="product_name" property="productName"/>
        <result column="product_image" property="productImage"/>
        <result column="unit_price" property="unitPrice"/>
        <result column="quantity" property="quantity"/>
        <result column="total_price" property="totalPrice"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!-- 新增订单项 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into order_item(order_id, product_id, product_name, product_image, unit_price, quantity, total_price, create_time, update_time)
        values (#{orderId}, #{productId}, #{productName}, #{productImage}, #{unitPrice}, #{quantity}, #{totalPrice}, #{createTime}, #{updateTime})
    </insert>

    <!-- 批量新增订单项 -->
    <insert id="batchInsert" useGeneratedKeys="true" keyProperty="id">
        insert into order_item(order_id, product_id, product_name, product_image, unit_price, quantity, total_price, create_time, update_time)
        values
        <foreach collection="orderItems" item="item" separator=",">
            (#{item.orderId}, #{item.productId}, #{item.productName}, #{item.productImage},
             #{item.unitPrice}, #{item.quantity}, #{item.totalPrice}, #{item.createTime}, #{item.updateTime})
        </foreach>
    </insert>

    <!-- 根据ID删除订单项（逻辑删除） -->
    <update id="deleteById">
        update order_item
        set quantity = 0, update_time = now()
        where id = #{id}
    </update>

    <!-- 根据订单ID批量删除订单项 -->
    <update id="deleteByOrderId">
        update order_item
        set quantity = 0, update_time = now()
        where order_id = #{orderId}
    </update>

    <!-- 条件更新订单项（动态更新） -->
    <update id="update">
        update order_item
        <set>
            <if test="orderId != null">
                order_id = #{orderId},
            </if>
            <if test="productId != null">
                product_id = #{productId},
            </if>
            <if test="productName != null and productName != ''">
                product_name = #{productName},
            </if>
            <if test="productImage != null and productImage != ''">
                product_image = #{productImage},
            </if>
            <if test="unitPrice != null">
                unit_price = #{unitPrice},
            </if>
            <if test="quantity != null">
                quantity = #{quantity},
            </if>
            <if test="totalPrice != null">
                total_price = #{totalPrice},
            </if>
            update_time = now()
        </set>
        where id = #{id}
    </update>

    <!-- 根据ID查询订单项 -->
    <select id="getById" resultMap="OrderItemResultMap">
        select id, order_id, product_id, product_name, product_image, unit_price, quantity, total_price, create_time, update_time
        from order_item
        where id = #{id}
        and quantity > 0
    </select>

    <!-- 多条件查询订单项列表 -->
    <select id="getList" resultMap="OrderItemResultMap">
        select id, order_id, product_id, product_name, product_image, unit_price, quantity, total_price, create_time, update_time
        from order_item
        <where>
            <if test="orderId != null">
                and order_id = #{orderId}
            </if>
            <if test="productId != null">
                and product_id = #{productId}
            </if>
            <if test="productName != null and productName != ''">
                and product_name like concat('%', #{productName}, '%')
            </if>
            <if test="quantity != null">
                and quantity = #{quantity}
            </if>
            <if test="unitPrice != null">
                and unit_price = #{unitPrice}
            </if>
            and quantity > 0
        </where>
        order by create_time desc
    </select>

    <!-- 根据ID批量查询订单项 -->
    <select id="getBatchIds" resultMap="OrderItemResultMap">
        select id, order_id, product_id, product_name, product_image, unit_price, quantity, total_price, create_time, update_time
        from order_item
        where id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        and quantity > 0
        order by create_time desc
    </select>

    <!-- 根据订单ID查询订单项列表 -->
    <select id="getByOrderId" resultMap="OrderItemResultMap">
        select id, order_id, product_id, product_name, product_image, unit_price, quantity, total_price, create_time, update_time
        from order_item
        where order_id = #{orderId}
        and quantity > 0
        order by create_time asc
    </select>

    <!-- 根据订单ID和商品ID查询订单项 -->
    <select id="getByOrderIdAndProductId" resultMap="OrderItemResultMap">
        select id, order_id, product_id, product_name, product_image, unit_price, quantity, total_price, create_time, update_time
        from order_item
        where order_id = #{orderId}
        and product_id = #{productId}
        and quantity > 0
        limit 1
    </select>

    <!-- 根据商品ID查询订单项列表 -->
    <select id="getByProductId" resultMap="OrderItemResultMap">
        select id, order_id, product_id, product_name, product_image, unit_price, quantity, total_price, create_time, update_time
        from order_item
        where product_id = #{productId}
        and quantity > 0
        order by create_time desc
    </select>

    <!-- 统计订单的商品总数 -->
    <select id="countTotalQuantity" resultType="java.lang.Integer">
        select coalesce(sum(quantity), 0)
        from order_item
        where order_id = #{orderId}
        and quantity > 0
    </select>

    <!-- 根据订单ID统计订单项数量 -->
    <select id="countByOrderId" resultType="java.lang.Integer">
        select count(*)
        from order_item
        where order_id = #{orderId}
        and quantity > 0
    </select>

</mapper>